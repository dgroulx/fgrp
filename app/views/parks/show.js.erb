var mapOptions = {
  center: new google.maps.LatLng(<%= "#{@park.latitude}, #{@park.longitude}" %>),
  zoom: 15,
  mapTypeId: google.maps.MapTypeId.ROADMAP,
  disableDefaultUI: true
};
var map = new google.maps.Map(document.getElementById("map_canvas"), mapOptions);
